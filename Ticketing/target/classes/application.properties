server.port=8080

# PostgreSQL Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/ticketing_db
spring.datasource.username=postgres
spring.datasource.password=innu

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# JWT Configuration
jwt.secret=ThisIsAReallyLongAndVerySecureSecretKeyForOurTicketingSystemProject12345
jwt.expiration=86400

# Allow Circular References (The Fix)
spring.main.allow-circular-references=true